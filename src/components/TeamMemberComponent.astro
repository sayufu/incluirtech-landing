---
const { photo, name, title, description, linkedinUrl } = Astro.props;
---

<div class="flex flex-col items-center bg-white shadow-lg rounded-lg p-4 md:p-8 text-center gap-2 h-full">
    <div class="flex flex-col items-center flex-grow">
        <img src={photo} alt={`Photo of ${name}`} class="w-24 h-24 md:w-36 md:h-36 rounded-full object-cover mb-4" />
        <h2 class="text-lg md:text-2xl font-bold">{name}</h2>
        <p class="text-primary text-sm font-semibold">{title}</p>
        <p class="text-black text-center mt-2">{description}</p>
    </div>
    <a href={linkedinUrl} target="_blank" class="p-1 mt-auto transition duration-300 ease-in-out transform hover:scale-105">
        <ion-icon name="logo-linkedin" class="text-2xl md:text-3xl text-[#0E76A8]" />
    </a>
</div>